apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-k8s-pipeline-data
  labels:
    app.kubernetes.io/name: docker-k8s-pipeline
    app.kubernetes.io/component: api
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
